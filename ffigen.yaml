# Run with `dart run ffigen --config ffigen.yaml`.
name: KokoroTtsBindings
description: |
  Bindings for `src/kokoro_tts.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/kokoro_tts_bindings_generated.dart'
headers:
  entry-points:
    - 'src/kokoro_tts.h'
  include-directives:
    - 'src/kokoro_tts.h'
comments:
  style: any
  length: full
